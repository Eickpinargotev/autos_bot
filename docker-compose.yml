name: autos

x-bot-environment: &bot-environment
  TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
  OPENAI_API_KEY: ${OPENAI_API_KEY}
  OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.4-mini}
  EVAL_JUDGE_MODEL: ${EVAL_JUDGE_MODEL:-gpt-4o-mini}
  POSTGRES_URL: postgresql://${POSTGRES_USER:-mi_usuario_db}:${POSTGRES_PASSWORD:-mi_password_seguro}@postgres:5432/${POSTGRES_DB:-mi_base_de_datos}
  REDIS_URL: redis://redis:6379/0
  QDRANT_URL: http://qdrant:6333
  RAG_CONVERSATION_HISTORY_LIMIT: ${RAG_CONVERSATION_HISTORY_LIMIT:-5}
  RAG_EMBEDDING_MODEL: ${RAG_EMBEDDING_MODEL:-text-embedding-3-small}
  RAG_SYNC_TTL_SECONDS: ${RAG_SYNC_TTL_SECONDS:-300}
  # WhatsApp (WasenderAPI). Sin credenciales el canal queda inactivo: el sender
  # falla con un mensaje claro y el webhook responde 503, nunca abierto.
  WASENDER_API_URL: ${WASENDER_API_URL:-https://wasenderapi.com}
  WASENDER_WEBHOOK_SECRET: ${WASENDER_WEBHOOK_SECRET:-}
  # Token compartido con el dashboard para los endpoints internos del bot.
  INTERNAL_API_TOKEN: ${INTERNAL_API_TOKEN:-}
  GOOGLE_DRIVE_IMAGE_DOWNLOAD_URL_TEMPLATE: ${GOOGLE_DRIVE_IMAGE_DOWNLOAD_URL_TEMPLATE:-https://drive.google.com/uc?export=download&id={image_id}}
  OUTBOUND_IMAGE_TIMEOUT_SECONDS: ${OUTBOUND_IMAGE_TIMEOUT_SECONDS:-30}
  OUTBOUND_IMAGE_MAX_BYTES: ${OUTBOUND_IMAGE_MAX_BYTES:-15000000}
  PUB_DELAY_1_SEC: ${PUB_DELAY_1_SEC}
  PUB_DELAY_2_SEC: ${PUB_DELAY_2_SEC}
  PUB_DELAY_3_SEC: ${PUB_DELAY_3_SEC}
  MESSAGE_BUFFER_SECONDS: ${MESSAGE_BUFFER_SECONDS:-15}
  MSG_DELAY_MIN: ${MSG_DELAY_MIN}
  MSG_DELAY_MAX: ${MSG_DELAY_MAX}

x-bot-build: &bot-build
  context: ./services/bot_agent
  dockerfile: Dockerfile
  target: prod

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mi_usuario_db}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mi_password_seguro}
      POSTGRES_DB: ${POSTGRES_DB:-mi_base_de_datos}
    volumes:
      - autos_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    # AOF (appendonly): persiste cada escritura a disco. Redis guarda el estado
    # activo de la conversación (en qué paso del flujo va cada usuario); con AOF
    # una caída dura pierde ~1s en vez de hasta varios minutos (solo RDB).
    #
    # Política de memoria: sin maxmemory Redis crece hasta que el kernel mata el
    # proceso (OOM) y se cae el bot entero. Con volatile-lru, al llegar al tope
    # se desalojan SOLO claves con TTL (estados de conversación inactivos, que
    # igual expirarían) y NUNCA las colas de Celery (no tienen TTL): degradación
    # suave en vez de caída. Ajustable con REDIS_MAXMEMORY en el .env.
    command: redis-server --appendonly yes --maxmemory ${REDIS_MAXMEMORY:-512mb} --maxmemory-policy volatile-lru
    volumes:
      - autos_redis_data:/data
    restart: unless-stopped

  qdrant:
    image: qdrant/qdrant:latest
    volumes:
      - autos_qdrant_data:/qdrant/storage
    restart: unless-stopped

  dashboard:
    build:
      context: ./services/dashboard
      dockerfile: Dockerfile
      target: prod
    environment:
      POSTGRES_URL: postgresql://${POSTGRES_USER:-mi_usuario_db}:${POSTGRES_PASSWORD:-mi_password_seguro}@postgres:5432/${POSTGRES_DB:-mi_base_de_datos}
      # OBLIGATORIO en producción: sin esto el servicio no arranca.
      SESSION_SECRET: ${SESSION_SECRET}
      COOKIE_SECURE: ${COOKIE_SECURE:-true}
      SESSION_TTL_HOURS: ${SESSION_TTL_HOURS:-12}
      # Cuenta de administrador. El .env manda: si cambias ADMIN_PASSWORD aquí,
      # el próximo arranque la aplica. Si la cambias desde el panel o la
      # recuperas por Telegram, el arranque NO la pisa.
      ADMIN_USER: ${ADMIN_USER:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      # Único destino del código de recuperación de la contraseña.
      ADMIN_TELEGRAM_CHAT_ID: ${ADMIN_TELEGRAM_CHAT_ID}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      ENVIO_DELAY_MIN_SEGUNDOS: ${ENVIO_DELAY_MIN_SEGUNDOS:-1}
      ENVIO_DELAY_MAX_SEGUNDOS: ${ENVIO_DELAY_MAX_SEGUNDOS:-6}
      BOT_WEBHOOK_URL: http://whatsapp_webhook:8010
      INTERNAL_API_TOKEN: ${INTERNAL_API_TOKEN:-}
      # Dominio público del servicio de webhooks. El panel lo usa para armar la
      # URL que se copia en WasenderAPI; sin él las URLs salen incompletas.
      PUBLIC_WEBHOOK_BASE_URL: ${PUBLIC_WEBHOOK_BASE_URL:-}
      ZONA_HORARIA: ${ZONA_HORARIA:-America/Costa_Rica}
    expose:
      - "8020"
    networks:
      - default
      - easypanel
    depends_on:
      - postgres
    restart: unless-stopped

  bot_agent:
    build: *bot-build
    environment: *bot-environment
    volumes:
      - ./mensajes.json:/mensajes.json:ro
    depends_on:
      - postgres
      - redis
      - qdrant
    restart: unless-stopped

  celery_worker:
    build: *bot-build
    environment: *bot-environment
    volumes:
      - ./mensajes.json:/mensajes.json:ro
    depends_on:
      - postgres
      - redis
      - qdrant
    # -B: corre el scheduler beat embebido (purga diaria del historial vencido).
    # -s: el archivo de agenda va a /tmp para no tocar el código montado.
    # --pool=threads --concurrency=20: las tareas son I/O-bound (esperan al LLM),
    #   no CPU-bound. Con hilos corren ~20 peticiones al LLM en paralelo sin gastar
    #   CPU (el GIL se libera durante la espera de red); 20 da ~2x de margen para
    #   ~200 chats/min. NO subir esto a prefork con 20 procesos: saturaría CPU/RAM.
    # --statedb: persiste las tareas revocadas (recordatorios cancelados). Sin
    #   esto, tras un reinicio del worker los recordatorios ya cancelados se
    #   volverían a entregar y ejecutar.
    command: celery -A src.infrastructure.tasks.celery_app worker -B -s /tmp/celerybeat-schedule --statedb=/tmp/celery-worker-state --pool=threads --concurrency=20 --loglevel=info
    restart: unless-stopped

  whatsapp_webhook:
    build: *bot-build
    environment: *bot-environment
    expose:
      - "8010"
    # Va en `easypanel` porque tiene que ser ALCANZABLE DESDE INTERNET: es la
    # URL que se pega en WasenderAPI. Necesita su propio dominio en EasyPanel,
    # igual que el dashboard. Lo que lo protege no es la red sino el token de
    # cada cliente (`/webhooks/wasender/<token>`): sin token válido, 401.
    networks:
      - default
      - easypanel
    volumes:
      - ./mensajes.json:/mensajes.json:ro
    depends_on:
      - postgres
      - redis
      - qdrant
    command: uvicorn src.infrastructure.webhooks.app:app --host 0.0.0.0 --port 8010
    restart: unless-stopped

volumes:
  autos_postgres_data:
    name: autos_postgres_data
  autos_redis_data:
    name: autos_redis_data
  autos_qdrant_data:
    name: autos_qdrant_data

networks:
  default:
  easypanel:
    external: true
